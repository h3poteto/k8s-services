apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: metallb
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      chart: metallb/metallb
      version: '0.0.0'
      sourceRef:
        kind: HelmRepository
        name: metallb
        namespace: default
      interval: 1m
