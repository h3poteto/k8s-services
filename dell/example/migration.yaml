apiVersion: batch/v1
kind: Job
metadata:
  name: migration
  namespace: example
  labels:
    app: migration
spec:
  template:
    metadata:
      labels:
        app: migration
    spec:
      containers:
        - name: busybox
          image: busybox
          args: ["echo", "migrating"]
      restartPolicy: Never
